package com.integraal.ops.integration.flow.configurations;

// ? The type of the integration that will be initialized with
public enum IntegrationType {
    SPRING_NATIVE,
    RABBITMQ,
    KAFKA,
    PULSAR,
}
